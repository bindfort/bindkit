# Bindkit Landing Page

This folder contains the standalone landing-page artifact for Bindkit.

The live Next.js route is implemented at:

- `src/app/bindkit/page.tsx`
- `src/app/bindkit/bindkit.css`

The static `index.html` file is kept here as a portable reference for the Bindkit project folder.

