# Weekend Launch Checklist

- Replace `tools/example_weather` with a boring B2B tool.
- Keep `initialize` and `tools/list` public.
- Gate only `tools/call`.
- Set real API keys through environment variables.
- Run `go test ./...`.
- Build the Docker image.
- Write one example client config.
- Post the repo with a concrete claim: "paid MCP server starter kit in Go".

